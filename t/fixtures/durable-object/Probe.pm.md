# Example::Probe

Local qualification fixture for isolation, awaited timers, transactional rollback,
capability expiry, call-cycle rejection, error propagation and reconstruction.
It deliberately exposes failure probes and must not be used as a production API.
Run through `t.js/qualify-durable-objects.mjs` with a packaged runtime.
